<UserControl x:Class="BDSM.BackupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BDSM"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid Margin="20">
    <StackPanel>
      <TextBlock Text="Server Backups" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

      <TextBlock Text="{Binding TimeUntilNextBackup}" Foreground="LimeGreen" FontSize="14" Margin="0,0,0,20"/>

      <TextBlock Text="Use this section to manually trigger a backup of all active servers." Foreground="LightGray" Margin="0,0,0,20" TextWrapping="Wrap" MaxWidth="600"/>

      <Button Command="{Binding StartManualBackupCommand}"
              Style="{DynamicResource MaterialDesignRaisedButton}"
              HorizontalAlignment="Left"
              ToolTip="This will save the world on all running servers and then create a backup .zip file for all active servers.">
        <StackPanel Orientation="Horizontal">
          <materialDesign:PackIcon Kind="DatabaseSyncOutline" Margin="0,0,10,0"/>
          <TextBlock Text="Start Manual Backup Now"/>
        </StackPanel>
      </Button>
    </StackPanel>
  </Grid>
</UserControl>