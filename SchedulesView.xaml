<UserControl x:Class="BDSM.SchedulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BDSM"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Scheduled Tasks" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <Border Style="{StaticResource CardBorderStyle}" Padding="5">
                <ListBox ItemsSource="{Binding ScheduledTasks}"
                         SelectedItem="{Binding SelectedTask}"
                         DisplayMemberPath="Name"
                         Height="300"/>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Add Task" Command="{Binding AddTaskCommand}"/>
                <Button Content="Remove Task" Command="{Binding RemoveTaskCommand}" Foreground="Red" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" IsEnabled="{Binding IsTaskSelected}">
            <GroupBox Header="Selected Task Details"
                      materialDesign:ThemeAssist.Theme="Dark"
                      Padding="10" BorderThickness="0">
                <StackPanel>
                    <TextBlock Text="Task Name:"/>
                    <TextBox Text="{Binding EditingName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                    <TextBlock Text="Task Type:"/>
                    <ComboBox ItemsSource="{Binding TaskTypes}" 
                              SelectedItem="{Binding EditingTaskType}" Margin="0,5,0,15">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="Black" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="Scheduled Time (HH:mm):"/>
                    <TextBox Text="{Binding EditingScheduledTime, UpdateSourceTrigger=PropertyChanged, StringFormat=hh\\:mm}" Margin="0,5,0,15"/>

                    <CheckBox IsChecked="{Binding EditingIsEnabled}" Content="Task Enabled" Margin="0,0,0,15"/>

                    <TextBlock Text="Run on Days:"/>
                    <UniformGrid Rows="1" Margin="0,5,0,15">
                        <CheckBox IsChecked="{Binding EditingRunsOnMonday}" Content="Mon" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnTuesday}" Content="Tue" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnWednesday}" Content="Wed" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnThursday}" Content="Thu" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnFriday}" Content="Fri" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnSaturday}" Content="Sat" Margin="0,0,5,0"/>
                        <CheckBox IsChecked="{Binding EditingRunsOnSunday}" Content="Sun" Margin="0,0,5,0"/>
                    </UniformGrid>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Next scheduled run:"/>
                        <TextBlock Text="{Binding SelectedTask.NextCalculatedRunTime, StringFormat='{}{0:g}'}" Foreground="White" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="{Binding TimeUntilNextRun}" Foreground="Gold" FontWeight="Bold" Margin="5,0,0,0"/>
                    </StackPanel>

                    <Button Content="Save Schedules"
                            Command="{Binding SaveSchedulesCommand}"
                            HorizontalAlignment="Left"
                            Margin="0,30,0,0"/>
                </StackPanel>
            </GroupBox>
        </Border>
    </Grid>
</UserControl>