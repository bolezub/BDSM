<UserControl x:Class="BDSM.ServerStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BDSM"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="1000">
    <Border Background="#2C2646" CornerRadius="10" Margin="0,5">
        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Fill="{Binding StatusColor}" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,15,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ServerName}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                    <TextBlock Text="{Binding Status}" Foreground="LightGray" FontSize="12"/>
                    <TextBlock Text="{Binding Pid}" Foreground="LightGray" FontSize="12"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ServerVersion}" Foreground="LightGray" FontSize="12" VerticalAlignment="Center"/>
                        <materialDesign:PackIcon Kind="CloudDownloadOutline" Foreground="Orange" Margin="5,0,0,0" VerticalAlignment="Center">
                            <materialDesign:PackIcon.Style>
                                <Style TargetType="materialDesign:PackIcon">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUpdateAvailable}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:PackIcon.Style>
                        </materialDesign:PackIcon>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Players:" Foreground="LightGray" HorizontalAlignment="Center"/>
                <TextBlock Foreground="White" FontSize="20" HorizontalAlignment="Center">
                     <Run Text="{Binding CurrentPlayers}"/>/<Run Text="60"/>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0" HorizontalAlignment="Center">
                <TextBlock Text="CPU" Foreground="LightGray"/>
                <TextBlock Text="{Binding CpuUsage, StringFormat='{}{0:F2}%'}" Foreground="White" FontSize="16"/>
            </StackPanel>

            <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0" HorizontalAlignment="Center">
                <TextBlock Text="RAM" Foreground="LightGray"/>
                <TextBlock Text="{Binding RamUsage, StringFormat='{}{0}GB'}" Foreground="White" FontSize="16"/>
            </StackPanel>

            <Button Grid.Column="4" 
                    Command="{Binding ShowGraphDetailCommand}"
                    Background="Transparent" 
                    BorderBrush="Transparent"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="20,0">
                <lvc:CartesianChart
                    Series="{Binding Series}"
                    XAxes="{Binding XAxes}"
                    YAxes="{Binding YAxes}"
                    Width="200"
                    Height="80"
                    IsHitTestVisible="False"
                    TooltipPosition="Hidden"
                    LegendPosition="Hidden"
                    ZoomMode="None">
                </lvc:CartesianChart>
            </Button>

            <Menu Grid.Column="5" VerticalAlignment="Center" Background="Transparent">
                <MenuItem Header="{materialDesign:PackIcon Kind=DotsVertical, Size=24}" ToolTip="Server Actions">
                    <MenuItem Header="Start" Command="{Binding StartServerCommand}"/>
                    <MenuItem Header="Gracefully Stop" Command="{Binding StopServerCommand}"/>
                    <MenuItem Header="Gracefully Restart" Command="{Binding RestartServerCommand}"/>
                    <Separator/>
                    <MenuItem Header="Save World" Command="{Binding SaveWorldCommand}"/>
                    <MenuItem Header="Send Message..." Command="{Binding SendMessageCommand}"/>
                    <MenuItem Header="Send RCON Command..." Command="{Binding SendGenericRconCommand}"/>
                    <Separator/>
                    <MenuItem Header="Kill Process" Command="{Binding KillProcessCommand}" Foreground="Red" FontWeight="Bold"/>
                </MenuItem>
            </Menu>
        </Grid>
    </Border>
</UserControl>