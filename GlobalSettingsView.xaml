<UserControl x:Class="BDSM.GlobalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BDSM"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="20,20,20,0" Style="{StaticResource ModernScrollViewer}">
            <StackPanel>
                <TextBlock Text="Global Settings" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                <TextBlock Text="Paths" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="250"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="SteamCMD Path:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SteamCMDPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                        <Button Grid.Row="0" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseSteamCMDCommand}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Backups Path:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                        <Button Grid.Row="1" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseBackupsPathCommand}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="GameUserSettings.ini Template Path:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding GameUserSettingsTemplatePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                        <Button Grid.Row="2" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseTemplatePathCommand}"/>
                    </Grid>
                </Border>

                <TextBlock Text="Server Connection &amp; API Keys" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="250"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Server IP Address:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ServerIP, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Discord Webhook URL (Notifications):" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding DiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Discord Webhook URL (Watchdog):" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding WatchdogDiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Discord Bot Token:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding BotToken, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <Button Grid.Row="3" Grid.Column="2" Command="{Binding ShowBotHelpCommand}"
                                ToolTip="Show Bot Setup Instructions"
                                Margin="5,0,0,0"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0"
                                Cursor="Hand">
                            <materialDesign:PackIcon Kind="HelpCircleOutline" Foreground="White"/>
                        </Button>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Discord Bot Prefix:" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding BotPrefix, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                    </Grid>
                </Border>

                <TextBlock Text="Server Launch Settings" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Start Arguments Template:" Margin="0,5,10,5"/>
                        <TextBox Text="{Binding StartArgumentsTemplate, UpdateSourceTrigger=PropertyChanged}"
                                 MinHeight="80"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"/>
                        <TextBlock Foreground="LightGray" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap">
                            <Run FontWeight="Bold">Available Variables:</Run><LineBreak/>
                            <Run>- {mapFolder}: Replaced with the server's selected map (e.g., TheIsland_WP).</Run><LineBreak/>
                            <Run>- {serverIP}: Replaced with the global Server IP Address.</Run><LineBreak/>
                            <Run>- {port}: Replaced with the server's Game Port.</Run><LineBreak/>
                            <Run>- {queryPort}: Replaced with the server's Query Port.</Run><LineBreak/>
                            <Run>- {clusterId}: Replaced with the parent cluster's ID.</Run>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <TextBlock Text="Scheduling Intervals &amp; Timeouts" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Backup Interval (Minutes):" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding BackupIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Update Check Interval (Minutes):" Margin="0,5,10,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding UpdateCheckIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Shutdown Timeout (Seconds):" Margin="0,5,10,5" ToolTip="How long to wait for a server to shut down gracefully before forcing it to close."/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ShutdownTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="SteamCMD Timeout (Minutes):" Margin="0,5,10,5" ToolTip="How long to wait for a SteamCMD update to finish before aborting it."/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SteamCmdTimeoutMinutes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                    </Grid>
                </Border>

                <TextBlock Text="Available Maps" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListBox Grid.Row="0" Margin="0,0,0,10" MaxHeight="200"
                                 ItemsSource="{Binding EditableAvailableMaps}"
                                 SelectedItem="{Binding SelectedMap}"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     materialDesign:HintAssist.Hint="Enter new map name"
                                     Text="{Binding NewMapName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Add" Margin="10,0,0,0" Command="{Binding AddMapCommand}"/>
                                <Button Content="Remove" Margin="5,0,0,0" Command="{Binding RemoveMapCommand}" Foreground="Red"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="1"
                Content="Save Global Settings"
                Command="{Binding SaveGlobalSettingsCommand}"
                HorizontalAlignment="Left"
                Margin="20,15,0,20"/>

    </Grid>
</UserControl>