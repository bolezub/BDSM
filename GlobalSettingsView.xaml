<UserControl x:Class="BDSM.GlobalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BDSM"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
        <StackPanel>
            <TextBlock Text="Global Settings" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <TextBlock Text="Paths" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,20,0,5"/>
            <Border BorderBrush="#444" BorderThickness="1" CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="250"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="SteamCMD Path:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SteamCMDPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                    <Button Grid.Row="0" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseSteamCMDCommand}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Backups Path:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                    <Button Grid.Row="1" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseBackupsPathCommand}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="GameUserSettings.ini Template Path:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding GameUserSettingsTemplatePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                    <Button Grid.Row="2" Grid.Column="2" Content="..." Margin="5,0,0,0" Command="{Binding BrowseTemplatePathCommand}"/>
                </Grid>
            </Border>

            <TextBlock Text="Server Connection &amp; API Keys" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,20,0,5"/>
            <Border BorderBrush="#444" BorderThickness="1" CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Server IP Address:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ServerIP, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Discord Webhook URL (Notifications):" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Discord Webhook URL (Watchdog):" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding WatchdogDiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Discord Bot Token:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding BotToken, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                </Grid>
            </Border>

            <TextBlock Text="Scheduling Intervals" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,20,0,5"/>
            <Border BorderBrush="#444" BorderThickness="1" CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Backup Interval (Minutes):" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding BackupIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Update Check Interval (Minutes):" VerticalAlignment="Center" Margin="0,5,10,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding UpdateCheckIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>
                </Grid>
            </Border>

            <TextBlock Text="Available Maps" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,20,0,5"/>
            <Border BorderBrush="#444" BorderThickness="1" CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10" MaxHeight="200"
                             ItemsSource="{Binding EditableAvailableMaps}"
                             SelectedItem="{Binding SelectedMap}"/>

                    <TextBox Grid.Row="1" Grid.Column="0" 
                             materialDesign:HintAssist.Hint="Enter new map name"
                             Text="{Binding NewMapName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Add" Margin="10,0,0,0" Command="{Binding AddMapCommand}"/>
                        <Button Content="Remove" Margin="5,0,0,0" Command="{Binding RemoveMapCommand}" Foreground="Red"/>
                    </StackPanel>

                </Grid>
            </Border>

            <Button Content="Save Global Settings"
                    Command="{Binding SaveGlobalSettingsCommand}"
                    HorizontalAlignment="Left"
                    Margin="0,30,0,0"
                    Style="{DynamicResource MaterialDesignRaisedButton}"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>