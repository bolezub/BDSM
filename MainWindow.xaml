<Window x:Class="BDSM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDSM"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Bole's Dedicated Server Manager" Height="720" Width="1280"
        Background="#1E1A2E"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:StatusBarViewModel}">
            <local:StatusBarView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:BackupViewModel}">
            <local:BackupsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:SchedulesViewModel}">
            <local:SchedulesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ClustersViewModel}">
            <local:ClustersView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:GlobalSettingsViewModel}">
            <local:GlobalSettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:WatchdogViewModel}">
            <local:WatchdogView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:ApplicationViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Clusters}" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,10">
                                <Border Background="#2C2646" CornerRadius="5,5,0,0" Padding="10,5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Start All" ToolTip="Start All Servers" Command="{Binding StartAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Stop All" ToolTip="Graceful Stop All Servers (15 min countdown)" Command="{Binding StopAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Restart All" ToolTip="Gracefully Restart All Servers (15 min countdown, includes update)" Command="{Binding RestartAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="E-Stop All" ToolTip="Emergency Stop All Servers (Immediate DoExit)" Command="{Binding EmergencyStopAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Save All" ToolTip="Save World on All Servers" Command="{Binding SaveAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Message All" ToolTip="Send a message to all servers" Command="{Binding MessageAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <ItemsControl ItemsSource="{Binding Servers}" Margin="10,0,10,10">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <local:ServerStatusView/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="#2C2646" Width="200">
            <TextBlock Text="bole's dedicated server manager" Foreground="White" FontWeight="Bold" Margin="20" FontSize="16" TextWrapping="Wrap"/>

            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,10,10,5" Command="{Binding ShowDashboardCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ViewDashboard" Margin="0,0,10,0"/>
                    <TextBlock Text="Dashboard"/>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5" Command="{Binding ShowClustersCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ServerNetwork" Margin="0,0,10,0"/>
                    <TextBlock Text="Clusters"/>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5" Command="{Binding ShowWatchdogCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Eye" Margin="0,0,10,0"/>
                    <TextBlock Text="Watchdog"/>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5" Command="{Binding ShowSchedulesCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ClockOutline" Margin="0,0,10,0"/>
                    <TextBlock Text="Schedules"/>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5" Command="{Binding ShowBackupsCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="DatabaseSyncOutline" Margin="0,0,10,0"/>
                    <TextBlock Text="Backups"/>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5" Command="{Binding ShowGlobalSettingsCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CogBox" Margin="0,0,10,0"/>
                    <TextBlock Text="Global Settings"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ContentControl Grid.Row="0" Grid.Column="1" Content="{Binding CurrentView}"/>

        <local:StatusBarView Grid.Row="1" Grid.Column="1" DataContext="{Binding StatusBar}"/>

        <materialDesign:Snackbar Grid.Row="0" Grid.Column="1" x:Name="MainSnackbar" MessageQueue="{materialDesign:MessageQueue}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="0,0,0,10"/>

    </Grid>

</Window>