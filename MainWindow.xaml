<Window x:Class="BDSM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDSM"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Bole's Dedicated Server Manager" Height="720" Width="1280"
        Background="#1E1A2E"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:StatusBarViewModel}">
            <local:StatusBarView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:BackupViewModel}">
            <local:BackupsView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:ApplicationViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Servers}" Margin="20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:ServerStatusView/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:SchedulesViewModel}">
            <local:SchedulesView/>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="#2C2646" Width="200">
            <TextBlock Text="bole's dedicated server manager" Foreground="White" FontWeight="Bold" Margin="20" FontSize="16" TextWrapping="Wrap"/>

            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,10,10,5"
                    Command="{Binding ShowDashboardCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ViewDashboard" Margin="0,0,10,0"/>
                    <TextBlock Text="Dashboard"/>
                </StackPanel>
            </Button>

            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5"
                    Command="{Binding ShowBackupsCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="DatabaseSyncOutline" Margin="0,0,10,0"/>
                    <TextBlock Text="Backups"/>
                </StackPanel>
            </Button>

            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5"
                    Command="{Binding ShowSchedulesCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ClockOutline" Margin="0,0,10,0"/>
                    <TextBlock Text="Schedules"/>
                </StackPanel>
            </Button>

            <Button Style="{DynamicResource MaterialDesignTextButton}" 
                    HorizontalAlignment="Left" Margin="10,5,10,5"
                    Command="{Binding CheckAllServersForUpdateCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CloudSyncOutline" Margin="0,0,10,0"/>
                    <TextBlock Text="Check for Updates"/>
                </StackPanel>
            </Button>

            <Button Style="{DynamicResource MaterialDesignTextButton}" 
                    HorizontalAlignment="Left" Margin="10,0,10,5"
                    Command="{Binding StartUpdateCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Update" Foreground="Orange" Margin="0,0,10,0"/>
                    <TextBlock Text="Start Update"/>
                </StackPanel>
            </Button>

            <Button Style="{DynamicResource MaterialDesignTextButton}" HorizontalAlignment="Left" Margin="10,5"
                    Command="{Binding ShowSettingsCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Cog" Margin="0,0,10,0"/>
                    <TextBlock Text="Settings"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ContentControl Grid.Row="0" Grid.Column="1" Content="{Binding CurrentView}"/>

        <local:StatusBarView Grid.Row="1" Grid.Column="1" DataContext="{Binding StatusBar}"/>

    </Grid>
</Window>