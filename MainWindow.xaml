<Window x:Class="BDSM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDSM"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Bole's Dedicated Server Manager" Height="720" Width="1280"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Color="#190f3c" Offset="0" />
            <GradientStop Color="#0c0722" Offset="1" />
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:StatusBarViewModel}">
            <local:StatusBarView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:BackupViewModel}">
            <local:BackupsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:SchedulesViewModel}">
            <local:SchedulesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ClustersViewModel}">
            <local:ClustersView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:GlobalSettingsViewModel}">
            <local:GlobalSettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:WatchdogViewModel}">
            <local:WatchdogView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ApplicationViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          Style="{StaticResource ModernScrollViewer}">
                <ItemsControl ItemsSource="{Binding Clusters}" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#44000000" CornerRadius="10" Margin="10" Padding="10">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.4" BlurRadius="10"/>
                                </Border.Effect>
                                <DockPanel>
                                    <Grid DockPanel.Dock="Top" Margin="5,0,5,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Start All" ToolTip="Start All Servers" Command="{Binding StartAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Stop All" ToolTip="Graceful Stop All Servers (15 min countdown)" Command="{Binding StopAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Restart All" ToolTip="Gracefully Restart All Servers (15 min countdown, includes update)" Command="{Binding RestartAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="E-Stop All" ToolTip="Emergency Stop All Servers (Immediate DoExit)" Command="{Binding EmergencyStopAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Save All" ToolTip="Save World on All Servers" Command="{Binding SaveAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                            <Button Content="Message All" ToolTip="Send a message to all servers" Command="{Binding MessageAllCommand}" Margin="2,0" Padding="8,2" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                    <Separator DockPanel.Dock="Top" Margin="5,0,5,5" Background="#44FFFFFF"/>
                                    <ItemsControl ItemsSource="{Binding Servers}" Margin="5,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <local:ServerStatusView/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DataTemplate>

        <Style x:Key="MainMenuButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="10, 12"/>
            <Setter Property="Margin" Value="15, 5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B345E"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border BorderThickness="1" BorderBrush="#4C4669">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.ColumnSpan="2" Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Click="MinimizeButton_Click" Style="{StaticResource WindowCloseButtonStyle}" Foreground="Gray">
                        <materialDesign:PackIcon Kind="WindowMinimize" />
                    </Button>
                    <Button Click="MaximizeButton_Click" Style="{StaticResource WindowCloseButtonStyle}" Foreground="Gray">
                        <materialDesign:PackIcon Kind="WindowMaximize" />
                    </Button>
                    <Button Click="CloseButton_Click" Style="{StaticResource WindowCloseButtonStyle}">
                        <materialDesign:PackIcon Kind="Close" />
                    </Button>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Width="200">
                <StackPanel.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#23105d" Offset="0" />
                        <GradientStop Color="#210f55" Offset="1" />
                    </LinearGradientBrush>
                </StackPanel.Background>

                <Image Source="/Assets/logo.png" Width="200" Height="200" Margin="0,0,0,0" Stretch="Uniform"/>

                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowDashboardCommand}" IsChecked="True" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ViewDashboard" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Dashboard" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowClustersCommand}" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ServerNetwork" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Clusters" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowWatchdogCommand}" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Eye" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Watchdog" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowSchedulesCommand}" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ClockOutline" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Schedules" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowBackupsCommand}" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="DatabaseSyncOutline" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Backups" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource MainMenuButton}" Command="{Binding ShowGlobalSettingsCommand}" GroupName="MainMenu">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CogBox" Margin="0,0,15,0" Width="20" Height="20"/>
                        <TextBlock Text="Global Settings" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
            </StackPanel>

            <ContentControl Grid.Row="1" Grid.Column="1" Content="{Binding CurrentView}"/>

            <local:StatusBarView Grid.Row="2" Grid.Column="1" DataContext="{Binding StatusBar}"/>

            <materialDesign:Snackbar Grid.Row="1" Grid.Column="1" x:Name="MainSnackbar" MessageQueue="{materialDesign:MessageQueue}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="0,0,0,10"/>

        </Grid>
    </Border>
</Window>